import pygame 

def staminabar:
    width = 100
    length = 200
    x = 250
    y=250
    
    def render(screen):
         pygame.draw.rect(screen, (0,0,0), pygame.Rect(x, y  , width, length))
         pygmae.draw.rect(screen, (0,0,0 ))
    def update(screen):
        render(screen)
